[package]
name = "lut_utility"
authors = ["Christian Schrinner"]
license = "MIT"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.5.4", features = ["derive"] }
image = { version = "0.25.5", features = ["png", "jpeg"] } # Ensure jpeg is included